meta {
  name: Remove folder
  type: http
  seq: 10
}

delete {
  url: {{assetsUrl}}/api/folder/:id
  body: none
  auth: inherit
}

params:path {
  id: {{tempTestFolderId}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
